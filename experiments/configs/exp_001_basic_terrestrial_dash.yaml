id: "exp_001_basic_terrestrial_dash"
description: "Basic DASH streaming over terrestrial network with static profile"

protocol: "dash"

# Replace SERVER_PUBLIC_IP with actual server IP when running
mpd_url: "http://SERVER_PUBLIC_IP:8080/dash/manifest.mpd"

network_profile:
  type: "static"
  file: "../../network_emulation/config/netem_profile_example.yaml"

experiment:
  duration_s: 120
  output_dir: "../../experiments/results/exp_001_basic_terrestrial_dash"

